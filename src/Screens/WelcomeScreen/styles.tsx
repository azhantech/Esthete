import {StyleSheet} from 'react-native';
import {vh, vw} from '../../Utils/helpers';

export const styles = StyleSheet.create({
  imageStyle: {height: vh * 65, width: vw * 100, resizeMode: 'stretch'},
});
